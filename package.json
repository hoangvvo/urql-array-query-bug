{
  "private": true,
  "name": "urql-array-query-bug",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "api": "node server"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "@graphql-tools/schema": "^7.1.2",
    "@urql/devtools": "^2.0.2",
    "@urql/exchange-graphcache": "^3.3.3",
    "express-graphql": "^0.12.0",
    "graphql": "^15.4.0",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "urql": "^1.11.4"
  }
}
